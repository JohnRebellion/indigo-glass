# Stage 4 — generation prompt, Gemini (paste into Gemini with image generation)

brand mark icon, flat vector style, orthographic straight-on view, square canvas

SILHOUETTE: single fused shape, circular body + 45-degree taper to small
rounded tip (ink-drop proportions), ALL edges horizontal/vertical/45-degree/
circular-arc ONLY, zero sharp corners, every join filleted round

INTERNAL DETAIL: one single straight diagonal negative-space cut along the
same 45-degree axis as the taper, background colour showing through, reads
as a leaf midrib. NOT a leaf outline. NOT a stem. NOT branching veins. Exactly
one straight line, nothing else inside the shape.

COLOUR: exactly two flat solid opaque tones, no third tone except optionally
one thin inner accent line that could be dropped without changing the
silhouette
- body fill: #C0E3C0 (pale muted sage green), completely flat, no gradient
- shadow duplicate: #89A889 (darker muted green), offset small step
  down-and-right, hard perfectly crisp edge, zero blur
- background: #07080A near black

MATERIAL: flat neobrutalist ink — opaque matte colour blocks, colour-as-
elevation via hard offset shadow only. explicitly NOT: glossy, NOT
translucent, NOT glass, NOT water droplet realism, NOT soft shadow, NOT
gradient, NOT ambient occlusion, NOT specular highlight, NOT 3D lighting

NEGATIVE: leaf illustration, serrated edge, botanical realism, herb bundle,
gradient fill, drop shadow blur, glossy sheen, glass material, translucency,
30-degree angle, 60-degree angle, sharp pointed corner, hamburger menu icon,
generic leaf-in-circle logo
