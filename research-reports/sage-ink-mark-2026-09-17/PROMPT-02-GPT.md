# Stage 4 — generation prompt, GPT (paste into ChatGPT with image generation)

A flat, opaque brand-mark icon on a near-black background (#07080A).

The silhouette is a single fused shape: a circular body tapering at exactly
45 degrees to a small rounded tip — like an ink drop, but every edge is
either horizontal, vertical, at exactly 45 degrees, or a perfect circular
arc. No other angles anywhere. No sharp corners — the tip and every join are
rounded, never pointed.

Cut through the shape, on the same 45-degree axis as the taper, one single
straight diagonal line as a thin negative-space channel (background colour
showing through) — this reads as a leaf's midrib. Do NOT draw a leaf outline,
stem, or any branching veins. Just the one straight cut.

Colour: exactly two flat, solid, matte tones. Body fill is pale sage green
(#C0E3C0). A hard-edged shadow duplicate of the same silhouette sits offset
one small step down and to the right, in a darker muted green (#89A889) —
the shadow has a perfectly crisp edge, zero blur, zero soft glow, and is
completely opaque.

Absolutely no gradients anywhere. No gloss, no specular highlight, no
translucency, no glass effect, no soft drop shadow, no ambient light. This is
a flat neobrutalist "ink" material: opaque colour blocks with hard offset
shadows as the only depth cue, like a sticker cut from two layers of matte
paper.

View straight-on, orthographic, no perspective tilt, no rotation in 3D.
Square canvas, the mark centred with generous margin, background pure
near-black.

If you would render this with a glossy water-droplet look, or add a leaf
stem/outline, or use a gradient or soft shadow — don't; flatten it back to
the flat two-tone description above.
